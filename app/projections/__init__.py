"""Where a projection came from, and the path for bringing your own.

`sources` names the three sources the draft room can be given numbers from
and answers the one question the paid one forces: may this viewer see them
(`docs/projection_sources.md`). `upload` reads a manager's own spreadsheet,
maps its columns to ours and stores it as a projection set the room can be
loaded from instead of Basketball Monster's export.
"""
